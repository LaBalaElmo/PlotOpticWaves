import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tordoya.plottingapp',
  appName: 'PlottingApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
